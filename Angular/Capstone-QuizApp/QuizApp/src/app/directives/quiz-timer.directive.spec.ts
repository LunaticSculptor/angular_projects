import { QuizTimerDirective } from './quiz-timer.directive';

describe('QuizTimerDirective', () => {
  it('should create an instance', () => {
    const directive = new QuizTimerDirective();
    expect(directive).toBeTruthy();
  });
});
