import { AnswerCorrectnessPipe } from './answer-correctness.pipe';

describe('AnswerCorrectnessPipe', () => {
  it('create an instance', () => {
    const pipe = new AnswerCorrectnessPipe();
    expect(pipe).toBeTruthy();
  });
});
